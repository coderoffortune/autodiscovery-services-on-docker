@model List<string>

@if (Model.Count > 0)
{
    <section class="errors">
        <h2>Errors</h2>
        <ul class="error-messages" role="list">
            @foreach (string message in Model)
            {
                <li>@message</li>
            }
        </ul>
    </section>
}

